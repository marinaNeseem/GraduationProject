const express = require('express');
const router = express.Router();
const { createExchangeRequest1,listOfferedItems } = require('../Controllers/RequestsController'); // Import as named import
router.route('/listUserItems').get(listOfferedItems);
router.route('/addrequest').post(createExchangeRequest1);

module.exports = router;
